<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature android:name="android.hardware.camera" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FONAApp"
        tools:targetApi="31">
        <activity
            android:name=".ui.detail.DetailMakananActivity"
            android:exported="false"/>

        <activity android:name=".ui.upload.CameraActivity"
            android:exported="false"/>
        <activity
            android:name=".ui.preferences.ResultUserPreferenceActivity"
            android:exported="false" />
        <activity
            android:name=".ui.login.ForgotPasswordActivity"
            android:exported="false" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".ui.daftar.DaftarActivity"
            android:exported="false" />
        <activity
            android:name=".ui.preferences.UserPreferenceActivity"
            android:exported="false" />
        <activity
            android:name=".ui.intro.WelcomeActivity"
            android:exported="false"/>


        <activity
            android:name=".main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>